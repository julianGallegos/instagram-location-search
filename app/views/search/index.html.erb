<p id="token" style="visibility: hidden"><%= session['access_token'] %></p>

<div class="container">
	<div class="row">
		<div class="col-md-12 text-center"><p>Welcome to Instagram Searcher</p></div>
	</div>
</div>

<div class="container">
	<div class="row">
		<div class="col-md-12 text-center">
					<p>Locations to search</p>
			<input id="locationSearch" type="text" name="locationSearch"><br><br>
			<button id="button" type="button">Search</button>
			<button id="reset" type="buton">Reset</button>
		</div>
	</div>
</div>

<div class="container top-buffer">
		<div class='results container'>
		</div>
</div>

<script>

	// $("#button").click(function(){
	// 	var lat = $('#lat').val();
	// 	var lon = $('#lon').val();
	// 	var token = "<%= session['access_token'] %>"
	// 	getResults(lat,lon,token);
	// 	console.log(token)
	// });

	// function getResults(lat,lon,token) {

	// 	var request = $.ajax({
	// 	  url: "/results",
	// 	  type: "POST",
	// 	  data: {lat:lat, lon:lon, access_token:token},
	// 	  dataType: "json"
	// 	});

	// 	request.done(function(msg) {
	// 		console.log("It worked");
	// 	  $("#button").html( msg );
	// 	});

	// 	request.fail(function(jqXHR, textStatus) {
	// 	  alert( "Request failed: " + textStatus );
	// 	});

	// }
 </script>